{
    "buildCommand": "cd ../.. && npx turbo run build --filter=klyver-master",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}